{
  "name": "minting-app",
  "homepage": "https://polymathnetwork.github.io/minting-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@polymathnetwork/react": "PolymathNetwork/polymath-react",
    "@polymathnetwork/sdk": "2.0.1-beta.61",
    "antd": "^3.23.2",
    "file-saver": "^2.0.2",
    "gh-pages": "^2.1.1",
    "rc-form-hooks": "^0.0.1-alpha.22",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.1.1"
  },
  "scripts": {
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build",
    "start": "NODE_OPTIONS=--max-old-space-size=4096 react-scripts start",
    "build": "NODE_OPTIONS=--max-old-space-size=4096 react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "husky": "^3.0.9"
  }
}
